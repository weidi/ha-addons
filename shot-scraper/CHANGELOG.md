# Changelog

## 0.1.1

- Added `auth_file` support for authenticated screenshots using a Playwright auth context file
- Added retry support for transient navigation failures such as `ERR_NETWORK_CHANGED`
- Updated automation examples to use the `app_start` then `app_stdin` flow

## 0.1.0

- Initial `shot-scraper` add-on release
- Added manual one-shot screenshot execution
- Added STDIN-based automation trigger support
